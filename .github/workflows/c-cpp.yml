name: C/C++ CI

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: goto merged
    - run: cd merged_code
    - name: make
      run: make
    - name: test
      run: ls -a | grep g
