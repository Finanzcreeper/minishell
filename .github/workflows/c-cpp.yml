name: C/C++ CI

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: update get
      run: apt-get update
    - name: install nextline
      run: sudo apt-get install lib32readline8 lib32readline-dev
    - name: make
      working-directory: merged_code
      run: make e
    - name: test
      run: ls -a | grep g
